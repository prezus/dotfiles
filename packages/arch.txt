# Arch/Omarchy package manifest — the pacman half of packages/bundle.
#
# ONLY what we add ON TOP of the OS. This is deliberately short: Omarchy already
# ships ~206 packages including the whole dev toolchain (bat, eza, fd, fzf,
# ripgrep, lazygit, docker, mise, tmux, starship, …). Re-declaring those would
# claim ownership of packages the distro maintains, and an `omarchy update` that
# changed one would surface as our drift.
#
# The baseline is read at runtime from /usr/share/omarchy/install/*.packages,
# so it stays correct across Omarchy upgrades without anything to maintain here.
# Before adding a line, check it is not already provided:
#   grep -x '<pkg>' /usr/share/omarchy/install/*.packages
#
# On plain Arch (no Omarchy) the baseline falls back to the base/base-devel
# groups, so this file legitimately needs to be longer there.

# ─── Shell & terminal ───────────────────────────────────────────────
fish
ghostty

# ─── Development ────────────────────────────────────────────────────
github-cli
just
zed

# ─── Language toolchains ────────────────────────────────────────────
#     Not in Omarchy's baseline, and packages/go.txt needs it. rust comes from
#     rustup's own installer instead (see the `rust` init step).
go
elixir
zig
jdk-openjdk
jupyterlab
#     Node itself is mise's, and bun has its own installer — these are the two
#     package managers projects pin in their lockfiles, nothing more.
pnpm
yarn
typescript

# ─── Language servers, linters, formatters ──────────────────────────
#     Editor-facing tools. rust-analyzer is absent on purpose: rustup ships it
#     as a component (packages/rust.txt), so a pacman copy would shadow the one
#     matching the active toolchain.
bash-language-server
lua-language-server
luacheck
stylua
prettier
typescript-language-server
golangci-lint

# ─── Required by the dotfiles CLI itself ────────────────────────────
#     `dotfiles pacman` also installs this during bootstrap, before the stow
#     step can run. Declared here so a re-run and the drift check agree.
stow

# ─── Security ───────────────────────────────────────────────────────
1password
1password-cli

# ─── Fonts ──────────────────────────────────────────────────────────
#     home-linux/.config/ghostty/personal.conf names Monaspace Neon/Xenon/Radon
#     and home/.config/zed/settings.json names Xenon/Neon. Without these the
#     configs stow cleanly and silently render in a fallback face.
#     One package per build, mirroring the four casks in packages/bundle:
#     static OTFs — "Monaspace Neon", "Monaspace Xenon", …
otf-monaspace
#     variable — "Monaspace Xenon Var"
ttf-monaspace-variable
#     Nerd Font patched, for devicon/powerline glyphs
otf-monaspace-nerd
#     static instances of the variable axes
ttf-monaspace-frozen
#     JetBrains Mono is already in Omarchy's baseline (ttf-jetbrains-mono-nerd-basic).

# ─── Applications ───────────────────────────────────────────────────
steam
discord
