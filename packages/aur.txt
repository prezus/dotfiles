# AUR packages, installed with `yay -S --needed`.
# Kept separate from arch.txt because yay must never run under sudo.
google-chrome
wootility

# Zero-trust VPN client. No official repo package; the vendor ships .deb/.rpm
# only, so the AUR PKGBUILD is the packaged route on Arch.
twingate
